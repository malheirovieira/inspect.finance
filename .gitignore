# dependencies
node_modules/

# build output
dist/
dist-ssr/
*.tsbuildinfo

# env files (nunca commitar)
.env
.env.local
.env.*.local

# logs
logs/
*.log
npm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# Claude Code — settings.local.json é config pessoal (allowlist de permissões),
# skills instalados via `npx skills add` são ferramenta local de quem está codando
.claude/
.agents/
skills-lock.json

# os
.DS_Store
Thumbs.db

# supabase local
supabase/.branches/
supabase/.temp/

# referência de design solta na raiz (já extraída pro app em src/pages/LandingPage.tsx
# e public/videos/) — não faz parte do build
nova_landing_page/
